import psycopg2 

connection =  psycopg2.connect(user = "postgres", password = "hmqp87k3", host = "database-2.cptcfbaucaoy.us-east-1.rds.amazonaws.com", port = "5432", database = "postgres")
cursor = connection.cursor()

cursor.execute(" DROP TABLE IF EXISTS test")

connection.commit()

q1 = """ CREATE TABLE test (id serial primary key, Collections VARCHAR(100) not null, LR_Floor VARCHAR(50), OS_Floor VARCHAR(50), Volume_24_HR VARCHAR(50), Percent_Difference VARCHAR(50)) """
cursor.execute(q1)